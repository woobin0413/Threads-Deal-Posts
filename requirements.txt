# Core dependencies
aiohttp==3.9.1        # For async HTTP requests to fetch deals from Reddit API
requests==2.31.0      # For synchronous HTTP requests to Threads API
python-dotenv==1.0.1  # For loading environment variables from .env file

# These are automatically installed with the above packages:
# - certifi: SSL certificates
# - charset-normalizer: Character encoding detection
# - idna: Internationalized domain names
# - urllib3: HTTP library used by requests
